# Examples

Repository-level integration and acceptance playbooks live here, grouped by
the module they demonstrate. They are intentionally outside `external/` so
each `external/<module>/` tree remains a pure installable NAT module and does
not imply that examples ship in its wheel.

Run commands from the repository root so paths and editable module installs in
the individual example READMEs resolve consistently.
