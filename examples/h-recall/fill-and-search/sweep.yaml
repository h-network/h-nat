functions:
  sweep:
    _type: h_semantic_sweep
    redis_url: ${H_NAT_REDIS_URL}
    pod: recall_demo
    agent: assistant

workflow:
  _type: h_semantic_sweep
  redis_url: ${H_NAT_REDIS_URL}
  pod: recall_demo
  agent: assistant
  migration_threshold_sec: 1
  max_per_cycle: 0
