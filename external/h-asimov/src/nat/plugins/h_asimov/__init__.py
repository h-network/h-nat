"""h-asimov — NAT plugin: pre-flight LLM safety judge.

Workflow-callable as ``_type: h_asimov_gate``. See ``register.py`` for
the function spec and ``../../../../LLD.md`` for the canonical
low-level design, including what was ported from
``h-network-asimov-firewall`` vs. newly built here.
"""
